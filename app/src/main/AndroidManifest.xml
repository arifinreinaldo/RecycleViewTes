<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daggertwo.tes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".explore.AchievementActivity"></activity>
        <activity android:name=".explore.UserActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".explore.MessageActivity" />
        <activity android:name=".explore.BaseActivity" />
        <activity android:name=".explore.RiwayatActivity" />
        <activity android:name=".explore.InfoActivity" />
        <activity android:name=".explore.TransactionActivity" />
        <activity android:name=".explore.DashboardActivity"></activity>
        <activity android:name=".CardActivity" />
        <activity android:name=".explore.SwipeActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>